---
interface Props {
  link: string;
}

const { link } = Astro.props;
---

<a href={link} class="transition font-semibold ease-in-out delay-100 hover:-translate-y-1 hover:scale-110 hover:text-orange-500 text__glowing">
  <slot />
</a>
